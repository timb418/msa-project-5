apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: export-pvc
  namespace: logistics
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
